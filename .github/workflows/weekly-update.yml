name: Update Crumbl Cookies

on:
  schedule:
    # Runs at 09:00 UTC every Monday (4:00 AM EST)
    - cron: '0 9 * * 1'
  workflow_dispatch: # Allows manual trigger

jobs:
  scrape-and-update:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install Dependencies
        run: |
          cd crumbl-notion-widget
          npm install

      - name: Create Service Account File
        # Creates the secret key inside the subfolder where the script lives
        run: |
          cd crumbl-notion-widget
          echo '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}' > service-account-key.json

      - name: Run Scraper
        # Enters the folder -> Runs the script
        run: |
          cd crumbl-notion-widget
          node scrape.js